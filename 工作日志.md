# 工作日志

## 第一阶段：后端单条日志分析闭环

### 完成时间

2026-06-05

### 阶段目标

第一阶段目标是先不接入前端、WAF、Redis、Milvus、LlamaIndex 和真实威胁情报，优先完成一个最小可运行的后端分析闭环。

目标链路：

```text
输入一条 WAF 日志 JSON
  -> 标准化为 SecurityEvent
  -> LogParserAgent 解析攻击特征
  -> DecisionAgent 判断攻击类型和风险等级
  -> FastAPI 返回 SecurityAlert 告警 JSON
```

### 已完成文件

```text
backend/app/models/event.py
backend/app/models/alert.py
backend/app/agents/log_parser_agent.py
backend/app/agents/decision_agent.py
backend/app/agents/orchestrator.py
backend/app/api/analyze.py
backend/app/main.py
backend/requirements.txt
backend/sample_event_sqli.json
```

### 已完成能力

```text
定义 SecurityEvent 标准化安全事件模型
定义 ParsedSecurityEvent 日志解析中间结果模型
定义 SecurityAlert 结构化告警模型
定义 MitreTechnique ATT&CK 映射模型
实现 LogParserAgent 攻击特征解析
实现 DecisionAgent 风险评分、ATT&CK 映射、处置建议和 Markdown 报告生成
实现 SecurityAnalysisOrchestrator 分析流程编排
实现 GET /api/health
实现 POST /api/analyze
```

### 第一阶段核心链路

```text
SecurityEvent
  -> LogParserAgent.parse()
  -> ParsedSecurityEvent
  -> DecisionAgent.decide()
  -> SecurityAlert
  -> POST /api/analyze
```

### 支持的攻击类型

```text
SQL Injection
XSS
Path Traversal
Command Injection
Automated Scanner
Unknown
```

### OWASP CRS 规则辅助判断

```text
942xxx -> SQL Injection
941xxx -> XSS
930xxx -> Path Traversal
932xxx -> Command Injection
913xxx -> Automated Scanner
```

### 风险等级规则

```text
0-39 -> low
40-69 -> medium
70-89 -> high
90-100 -> critical
```

### MITRE ATT&CK 映射

```text
SQL Injection -> T1190 Exploit Public-Facing Application
Path Traversal -> T1190 Exploit Public-Facing Application
XSS -> T1189 Drive-by Compromise
Command Injection -> T1059 Command and Scripting Interpreter
```

### 第一阶段测试用例

#### SQL 注入

请求接口：

```text
POST /api/analyze
```

请求体：

```json
{
  "event_id": "evt-demo-0001",
  "source_ip": "45.67.89.10",
  "method": "GET",
  "url": "/login?id=1' OR '1'='1",
  "path": "/login",
  "query": "id=1' OR '1'='1",
  "status": 403,
  "user_agent": "sqlmap/1.7",
  "waf_rule_id": "942100",
  "waf_message": "SQL Injection Attack Detected",
  "raw_log": "GET /login?id=1' OR '1'='1 HTTP/1.1"
}
```

预期结果：

```text
attack_type: SQL Injection
risk_level: high 或 critical
source_ip: 45.67.89.10
target: /login
confidence: 0.8 以上
mitre_attack: T1190
evidence: 包含 SQL 注入特征、sqlmap 特征、WAF 规则命中、WAF 告警信息
recommendations: 包含参数化查询检查、WAF 规则加固、源 IP 回溯、证据保留
report_markdown: 非空
```

实际结果：

```text
已通过。
```

#### XSS

请求体：

```json
{
  "event_id": "evt-demo-0002",
  "source_ip": "8.8.8.8",
  "method": "GET",
  "url": "/search?q=<script>alert(1)</script>",
  "path": "/search",
  "query": "q=<script>alert(1)</script>",
  "status": 403,
  "user_agent": "Mozilla/5.0",
  "waf_rule_id": "941100",
  "waf_message": "XSS Attack Detected",
  "raw_log": "GET /search?q=<script>alert(1)</script> HTTP/1.1"
}
```

预期结果：

```text
attack_type: XSS
risk_level: medium 或 high
mitre_attack: T1189
report_markdown: 非空
```

实际结果：

```text
已通过。
```

#### 路径穿越

请求体：

```json
{
  "event_id": "evt-demo-0003",
  "source_ip": "9.9.9.9",
  "method": "GET",
  "url": "/download?file=../../etc/passwd",
  "path": "/download",
  "query": "file=../../etc/passwd",
  "status": 403,
  "user_agent": "Mozilla/5.0",
  "waf_rule_id": "930120",
  "waf_message": "Path Traversal Attack Detected",
  "raw_log": "GET /download?file=../../etc/passwd HTTP/1.1"
}
```

预期结果：

```text
attack_type: Path Traversal
risk_level: high
mitre_attack: T1190
report_markdown: 非空
```

实际结果：

```text
已通过。
```

### 第一阶段调试记录

1. `LogParserAgent` 最初继承 `BaseModel`，导致 Pydantic 将规则列表识别为模型字段。

解决方式：

```text
LogParserAgent 是业务逻辑类，不是数据模型，因此改为普通 class。
```

2. `DecisionAgent` 方法需要确保位于类内部。

解决方式：

```text
保证 decide()、_score()、_level()、_map_mitre()、_recommendations()、_build_report() 都缩进在 DecisionAgent 内部。
```

3. 中文注释曾出现编码异常。

解决方式：

```text
Python 文件和 Markdown 文件统一使用 UTF-8 保存。
```

### 第一阶段结论

第一阶段已完成。

系统已经具备最小后端分析能力：

```text
输入一条安全日志 JSON
  -> 自动识别攻击类型
  -> 自动计算风险等级
  -> 自动生成证据列表
  -> 自动映射 MITRE ATT&CK
  -> 自动生成处置建议
  -> 自动生成 Markdown 报告
```

## 第二阶段：样例日志回放链路

### 完成时间

2026-06-05

### 阶段目标

第二阶段目标是把第一阶段的“手动提交单条 JSON 日志”升级为“从样例日志文件批量读取、解析并生成多条告警”。

目标链路：

```text
data/sample_logs/*.log
  -> ModSecurityParser.parse_line()
  -> SecurityEvent
  -> SecurityAnalysisOrchestrator.analyze()
  -> SecurityAlert
  -> 控制台输出
  -> reports/*.md
```

### 已完成文件

```text
data/sample_logs/sqli.log
data/sample_logs/xss.log
data/sample_logs/path_traversal.log
data/sample_logs/normal.log
backend/app/collector/__init__.py
backend/app/collector/modsecurity_parser.py
scripts/replay_sample_logs.py
reports/*.md
第二阶段任务规划.md
```

### 已完成能力

```text
读取样例日志文件
解析 key=value 格式的简化 WAF 日志
将原始日志转换为 SecurityEvent
复用第一阶段 Orchestrator 生成 SecurityAlert
在控制台输出攻击类型、风险等级、源 IP、目标路径和风险分数
将每条告警的 Markdown 报告写入 reports 目录
```

### ModSecurityParser 完成情况

已完成 `ModSecurityParser`。

该模块作用：

```text
原始日志字符串 -> SecurityEvent
```

已完成方法：

```text
parse_line()
_extract_fields()
_split_url()
```

当前支持的样例日志格式：

```text
client_ip=45.67.89.10 method=GET url="/login?id=1' OR '1'='1" status=403 user_agent="sqlmap/1.7" rule_id=942100 message="SQL Injection Attack Detected"
```

字段提取结果包括：

```text
source_ip
method
url
path
query
status
user_agent
waf_rule_id
waf_message
raw_log
```

### 日志回放脚本完成情况

已完成 `scripts/replay_sample_logs.py`。

脚本运行方式：

```bash
python scripts/replay_sample_logs.py
```

脚本处理流程：

```text
1. 读取 data/sample_logs/*.log
2. 过滤空行
3. 使用 ModSecurityParser 解析每一行日志
4. 使用 SecurityAnalysisOrchestrator 生成告警
5. 打印告警摘要
6. 将 report_markdown 写入 reports/{alert_id}.md
```

### 第二阶段测试完成情况

已完成 4 类样例日志回放测试：

```text
SQL Injection
XSS
Path Traversal
Normal Request
```

`ModSecurityParser` 单行解析测试已通过。

SQL 注入日志解析结果要点：

```text
source_ip: 45.67.89.10
method: GET
url: /login?id=1' OR '1'='1
path: /login
query: id=1' OR '1'='1
status: 403
user_agent: sqlmap/1.7
waf_rule_id: 942100
waf_message: SQL Injection Attack Detected
```

样例日志回放脚本已成功读取 4 条日志：

```text
normal.log
path_traversal.log
sqli.log
xss.log
```

### 第二阶段验收结果

第二阶段已通过。

验收项：

```text
data/sample_logs 中存在 4 类样例日志
ModSecurityParser 能将日志行解析为 SecurityEvent
replay_sample_logs.py 能批量读取样例日志
每条攻击日志都能生成 SecurityAlert
控制台能输出攻击类型、风险等级、源 IP 和目标路径
reports 目录能保存 Markdown 安全事件报告
正常请求能进入分析链路并输出低风险或 Unknown 结果
```

### 第二阶段调试记录

本阶段确认了脚本和后端包路径的关系：

```text
replay_sample_logs.py 位于 scripts/
后端包位于 backend/app/
```

因此脚本中需要将 `backend/` 加入 Python 导入路径，才能导入：

```python
from app.collector.modsecurity_parser import ModSecurityParser
from app.agents.orchestrator import SecurityAnalysisOrchestrator
```

### 第二阶段结论

第二阶段已完成。

系统已经从“手动 Swagger JSON 测试”升级为：

```text
样例日志文件批量回放
  -> 日志解析
  -> 自动分析
  -> 告警生成
  -> Markdown 报告输出
```

## 第三阶段计划：Redis Stream 实时事件流

下一阶段建议进入 Redis Stream 实时事件流。

目标链路：

```text
日志采集器
  -> Redis Stream: security:events
  -> 后端 Consumer
  -> Agent 分析
  -> Redis Stream: security:alerts
```

第三阶段完成后，再继续接入：

```text
真实 WAF 日志
LlamaIndex RAG
威胁情报
前端 SOC 控制台
```

## 第三阶段：Redis Stream 实时事件流

### 完成时间

2026-06-05

### 阶段目标

第三阶段目标是把第二阶段的“样例日志批量回放”升级为“基于 Redis Stream 的实时事件流”。

目标链路：

```text
data/sample_logs/*.log
  -> publish_sample_logs.py
  -> Redis Stream: security:events
  -> event_consumer.py
  -> SecurityAnalysisOrchestrator
  -> Redis Stream: security:alerts
  -> GET /api/alerts/recent
```

### 已完成文件

```text
docker-compose.yml
backend/requirements.txt
backend/app/storage/__init__.py
backend/app/storage/redis_client.py
backend/app/services/__init__.py
backend/app/services/event_consumer.py
backend/app/api/alerts.py
scripts/publish_sample_logs.py
第三阶段任务规划.md
```

### 已完成能力

```text
使用 Docker Compose 启动 Redis
将样例日志发布到 Redis Stream: security:events
从 Redis Stream: security:events 消费安全事件
将 Redis 中的 JSON 事件反序列化为 SecurityEvent
复用 SecurityAnalysisOrchestrator 生成 SecurityAlert
将安全告警写入 Redis Stream: security:alerts
通过 GET /api/alerts/recent 查询最近告警
```

### Redis Stream 设计

当前使用的 Stream：

```text
security:events
security:alerts
security:deadletter
```

作用说明：

```text
security:events - 待分析安全事件
security:alerts - 已生成安全告警
security:deadletter - 后续用于保存失败事件
```

### Redis 客户端完成情况

已完成 `backend/app/storage/redis_client.py`。

核心函数：

```text
get_redis_client()
publish_event()
publish_alert()
read_recent_alerts()
```

当前序列化方式：

```text
SecurityEvent.model_dump_json()
SecurityAlert.model_dump_json()
SecurityAlert.model_validate_json()
```

### 样例日志发布脚本完成情况

已完成 `scripts/publish_sample_logs.py`。

运行方式：

```bash
python scripts/publish_sample_logs.py
```

功能：

```text
读取 data/sample_logs/*.log
使用 ModSecurityParser 解析日志行为 SecurityEvent
调用 publish_event() 写入 security:events
打印 Redis Stream message id 和事件摘要
```

测试输出示例：

```text
published event 1780650610848-0: 127.0.0.1 -> /index
published event 1780650610864-0: 9.9.9.9 -> /download
published event 1780650610879-0: 45.67.89.10 -> /login
published event 1780650610895-0: 8.8.8.8 -> /search
```

### Consumer 完成情况

已完成 `backend/app/services/event_consumer.py`。

运行方式：

```bash
cd backend
python -m app.services.event_consumer
```

功能：

```text
从 security:events 读取事件
使用 SecurityEvent.model_validate_json() 还原事件
调用 SecurityAnalysisOrchestrator.analyze()
生成 SecurityAlert
写入 security:alerts
返回并打印处理数量
```

测试结果：

```text
processed events: 4
```

### 告警查询接口完成情况

已完成 `backend/app/api/alerts.py`。

接口：

```text
GET /api/alerts/recent
```

查询参数：

```text
count - 返回的最大告警数量，默认 20，范围 1 到 100
```

返回：

```text
list[SecurityAlert]
```

已在 `backend/app/main.py` 中注册：

```text
app.include_router(alerts_router)
```

### 第三阶段测试完成情况

已完成端到端测试：

```text
1. docker compose up -d redis
2. python scripts/publish_sample_logs.py
3. cd backend
4. python -m app.services.event_consumer
5. GET /api/alerts/recent
```

验证结果：

```text
security:events 中成功写入 4 条事件
event_consumer 成功处理 4 条事件
security:alerts 中成功写入 4 条告警
GET /api/alerts/recent 成功返回 4 条告警
```

返回的告警类型：

```text
XSS -> high
SQL Injection -> critical
Path Traversal -> high
Unknown -> low
```

### 中文输出修复记录

第三阶段测试中曾发现 API 返回内容出现乱码。经确认，接口本身正常，问题来自旧版本生成的告警文本已经写入 Redis。

处理方式：

```text
将 LogParserAgent 的 evidence 输出恢复为中文
将 DecisionAgent 的 recommendations 和 report_markdown 恢复为中文
清理 Redis 中旧的 security:events 和 security:alerts
重新发布样例日志
重新消费生成告警
重新验证 GET /api/alerts/recent
```

最终验证结果：

```text
API 返回中文内容正常。
```

中文返回示例：

```text
命中 XSS 特征：(?i)<script
检查 /search 的输入过滤和输出编码策略。
回溯源 IP 8.8.8.8 在过去 24 小时内的访问行为。
```

### 第三阶段验收结果

第三阶段已通过。

验收项：

```text
Redis 能通过 Docker Compose 启动
样例日志能写入 security:events
Consumer 能从 security:events 消费事件
Consumer 能生成 SecurityAlert
告警能写入 security:alerts
GET /api/alerts/recent 能返回最近告警
SQLi、XSS、路径穿越、正常请求均能通过 Redis 流完成分析
```

### 当前阶段结论

第三阶段已完成。

系统已经从“样例日志文件批量回放”升级为：

```text
样例日志
  -> Redis 实时事件流
  -> 后端消费分析
  -> Redis 告警流
  -> API 查询告警
```

### 下一阶段计划

下一阶段建议进入第四阶段：真实 WAF 日志接入。

目标链路：

```text
Nginx + ModSecurity + OWASP CRS
  -> WAF audit log
  -> waf_log_collector.py
  -> Redis Stream: security:events
  -> event_consumer.py
  -> Redis Stream: security:alerts
```

## 第五阶段工作记录：RAG 安全知识库增强

### 阶段目标

第五阶段目标是将前四阶段已经打通的 WAF 日志分析链路升级为带本地安全知识库引用的 RAG 增强分析链路。

目标链路：

```text
SecurityEvent
  -> LogParserAgent
  -> ParsedSecurityEvent
  -> DecisionAgent
  -> SecurityAlert
  -> RAGAgent
  -> 本地安全知识库
  -> 带知识库引用的 SecurityAlert
```

### 新增知识库

已新增本地 Markdown 安全知识库目录：

```text
backend/app/data/knowledge_base/
```

知识库文件：

```text
attack_patterns.md
mitre_attack.md
owasp_crs.md
remediation.md
cve_examples.md
```

覆盖内容：

```text
SQL Injection
XSS
Path Traversal
Command Injection
Automated Scanner
OWASP CRS 913 / 930 / 941 / 942 / 932
MITRE ATT&CK T1190 / T1189 / T1059
常见修复建议
CVE 类型示例
```

### 新增 RAG 模块

已新增目录：

```text
backend/app/rag/
```

新增文件：

```text
schemas.py
query_rewriter.py
knowledge_loader.py
rag_agent.py
```

模块职责：

```text
schemas.py - 定义 KnowledgeChunk、KnowledgeReference、RAGAnalysisResult
query_rewriter.py - 将 ParsedSecurityEvent 改写为适合检索的安全查询
knowledge_loader.py - 加载 Markdown 知识库并切分为知识块
rag_agent.py - 执行轻量本地检索、生成知识库引用和补充建议
```

### 编排链路接入

已修改：

```text
backend/app/agents/orchestrator.py
```

当前分析流程：

```text
parsed = LogParserAgent.parse(event)
alert = DecisionAgent.decide(parsed)
alert = RAGAgent.enrich_alert(alert, parsed)
```

因此以下入口都会自动获得 RAG 增强：

```text
POST /api/analyze
event_consumer.py
WAF collector -> Redis -> consumer
```

### RAG 增强内容

RAGAgent 会向最终 `SecurityAlert` 增加：

```text
知识库引用 evidence
知识库补充 recommendations
report_markdown 中的 RAG 知识库增强小节
```

报告新增小节：

```text
## RAG 知识库增强

**知识库引用**

**知识库补充建议**
```

### 验证结果

已使用三类核心攻击事件进行直接分析验证：

```text
SQL Injection -> 命中 OWASP CRS 942 SQL Injection、SQL Injection、CVE 类别：SQL 注入漏洞
XSS -> 命中 OWASP CRS 941 XSS、XSS、T1189 Drive-by Compromise
Path Traversal -> 命中 OWASP CRS 930 Path Traversal、Path Traversal、CVE 类别：路径穿越漏洞
```

验证结论：

```text
SQLi / XSS / Path Traversal 均能检索到相关知识库条目
SecurityAlert.evidence 中包含知识库引用
report_markdown 中包含 RAG 知识库增强内容
系统不依赖外部 API，也能完成本地知识增强分析
```

### 编译检查

已执行：

```text
D:\anaconda\envs\machinelearning\python.exe -m compileall backend\app
```

结果：

```text
编译通过
```

### 当前阶段结论

第五阶段 MVP 已完成。

系统已从：

```text
WAF 日志 -> 攻击识别 -> 基础告警
```

升级为：

```text
WAF 日志 -> 攻击识别 -> RAG 本地安全知识检索 -> 带引用的增强告警
```

## 第六阶段工作记录：威胁情报与历史记忆

### 阶段目标

第六阶段目标是将系统从“单条事件分析”升级为“带上下文的安全研判”。

目标链路：

```text
SecurityEvent
  -> LogParserAgent
  -> DecisionAgent
  -> RAGAgent
  -> ThreatIntelAgent
  -> EventMemory
  -> 综合研判
  -> SecurityAlert
```

### 新增威胁情报模块

已新增目录：

```text
backend/app/intel/
```

新增文件：

```text
__init__.py
schemas.py
local_intel_store.py
threat_intel_agent.py
```

模块职责：

```text
schemas.py - 定义 ThreatIntelResult
local_intel_store.py - 读取本地 mock 威胁情报 JSON
threat_intel_agent.py - 查询 IP 情报并增强 SecurityAlert
```

### 新增本地威胁情报库

已新增：

```text
backend/app/data/intel/threat_ip_mock.json
```

包含示例 IP：

```text
45.67.89.10 -> malicious / scanner / sqlmap / botnet
8.8.8.8 -> unknown / public_dns
9.9.9.9 -> suspicious / path-traversal-probe
172.23.0.1 -> demo_internal / docker-host / waf-demo-source
```

### 新增 Memory 模块

已新增目录：

```text
backend/app/memory/
```

新增文件：

```text
__init__.py
schemas.py
event_memory.py
```

模块职责：

```text
schemas.py - 定义 IpMemorySummary
event_memory.py - 使用 Redis 记录和查询源 IP 历史行为
```

Memory 使用的 Redis Key：

```text
memory:ip:{source_ip}
memory:ip:{source_ip}:attack_types
memory:ip:{source_ip}:targets
memory:ip:{source_ip}:alerts
```

记录内容：

```text
历史告警数量
最近一次告警
历史攻击类型
历史目标路径
最近告警 JSON
```

### 编排链路接入

已修改：

```text
backend/app/agents/orchestrator.py
```

当前完整分析流程：

```text
parsed = LogParserAgent.parse(event)
alert = DecisionAgent.decide(parsed)
alert = RAGAgent.enrich_alert(alert, parsed)
intel = ThreatIntelAgent.lookup(alert.source_ip)
alert = ThreatIntelAgent.enrich_alert(alert, intel)
memory_summary = EventMemory.get_ip_summary(alert.source_ip)
alert = EventMemory.enrich_alert(alert, memory_summary)
alert = _append_context_judgment(alert, intel, memory_summary)
EventMemory.record_alert(alert)
```

### 告警增强内容

第六阶段会向最终 `SecurityAlert` 增加：

```text
威胁情报 evidence
历史行为 evidence
综合研判 evidence
威胁情报 report_markdown 小节
历史行为 report_markdown 小节
综合研判 report_markdown 小节
基于情报和历史行为的风险分调整
```

报告新增小节：

```text
## 威胁情报

## 历史行为

## 综合研判
```

### 验证结果

已使用同一源 IP `45.67.89.10` 连续触发三类攻击：

```text
SQL Injection
XSS
Path Traversal
```

验证结果：

```text
SQL Injection -> critical，包含 local_mock / malicious / Memory 上下文
XSS -> critical，包含 local_mock / malicious / Memory 上下文
Path Traversal -> critical，包含 local_mock / malicious / Memory 上下文
```

Redis Memory 验证结果：

```text
memory:ip:45.67.89.10 alert_count = 3
attack_types = SQL Injection, XSS, Path Traversal
targets = /login, /search, /download
```

说明系统已经可以识别：

```text
同一 IP 多次触发不同类型攻击
该 IP 命中本地威胁情报
当前告警属于持续攻击或自动化探测上下文
```

### 编译检查

已执行：

```text
D:\anaconda\envs\machinelearning\python.exe -m compileall backend\app
```

结果：

```text
编译通过
```

### 当前阶段结论

第六阶段 MVP 已完成。

系统已从：

```text
WAF 日志 -> 攻击识别 -> RAG 知识增强
```

升级为：

```text
WAF 日志 -> 攻击识别 -> RAG 知识增强 -> 威胁情报 -> 历史记忆 -> 综合研判
```

## 第七阶段工作记录：前端 SOC 控制台

### 阶段目标

第七阶段目标是将后端已经生成的结构化安全告警通过前端 SOC 控制台展示出来。

目标链路：

```text
Redis security:alerts
  -> GET /api/alerts/recent
  -> frontend React SOC Console
  -> 告警列表
  -> 告警详情
  -> Markdown 报告
```

### 新增前端项目

已新增目录：

```text
frontend/
```

技术栈：

```text
React
TypeScript
Vite
lucide-react
react-markdown
```

核心文件：

```text
frontend/package.json
frontend/vite.config.ts
frontend/tsconfig.json
frontend/src/main.tsx
frontend/src/App.tsx
frontend/src/styles/index.css
```

### 新增前端类型与 API

新增：

```text
frontend/src/types/alert.ts
frontend/src/api/alerts.ts
```

能力：

```text
定义 SecurityAlert / MitreTechnique / RiskLevel 前端类型
请求 GET /api/alerts/recent
支持 VITE_API_BASE_URL 配置后端地址
默认后端地址为 http://127.0.0.1:8000
```

### 新增前端组件

新增：

```text
frontend/src/components/RiskBadge.tsx
frontend/src/components/StatBar.tsx
frontend/src/components/AlertTable.tsx
frontend/src/components/AlertDetail.tsx
frontend/src/components/ReportPanel.tsx
```

组件职责：

```text
RiskBadge - 展示 low / medium / high / critical 风险等级
StatBar - 统计总告警数、critical、high、源 IP 数、更新时间
AlertTable - 展示告警列表并支持点击选择
AlertDetail - 展示告警详情、证据、建议和 ATT&CK 映射
ReportPanel - 使用 react-markdown 渲染 Markdown 分析报告
```

### 后端 CORS 支持

已修改：

```text
backend/app/main.py
```

新增：

```text
CORSMiddleware
```

允许前端开发地址：

```text
http://127.0.0.1:5173
http://localhost:5173
```

### 页面展示能力

第七阶段前端可以展示：

```text
告警列表
风险等级
攻击类型
源 IP
目标路径
置信度
关键证据
处置建议
MITRE ATT&CK 映射
RAG 知识库增强
威胁情报
历史行为
综合研判
Markdown 报告
```

### 验证结果

已执行依赖安装：

```text
cd frontend
npm install
```

结果：

```text
found 0 vulnerabilities
```

已执行前端构建：

```text
npm run build
```

结果：

```text
TypeScript 编译通过
Vite build 通过
```

已执行后端编译检查：

```text
D:\anaconda\envs\machinelearning\python.exe -m compileall backend\app
```

结果：

```text
编译通过
```

已启动服务并验证：

```text
backend -> http://127.0.0.1:8000
frontend -> http://127.0.0.1:5173
GET /api/health -> 200
GET /api/alerts/recent?count=4 -> 返回 4 条告警
frontend / -> 200
```

### 当前阶段结论

第七阶段 MVP 已完成。

系统已从：

```text
后端 API / Swagger 查看告警
```

升级为：

```text
前端 SOC 控制台查看告警列表、详情、RAG 报告、威胁情报和历史行为
```

## 第八阶段工作记录：工程化收尾与演示交付

### 阶段目标

第八阶段目标是将项目整理为可以稳定演示、可以通过文档理解、可以用测试验证的完整交付形态。

目标内容：

```text
README
架构文档
演示文档
API 文档
故障排查文档
Redis 清理脚本
核心测试用例
最终验证流程
```

### 新增 README

已新增：

```text
README.md
```

内容包括：

```text
项目简介
核心亮点
系统架构
技术栈
快速启动
WAF 演示流程
API 说明
目录结构
验证命令
当前阶段状态
```

### 新增 docs 文档

已新增：

```text
docs/architecture.md
docs/demo_guide.md
docs/api_reference.md
docs/troubleshooting.md
```

文档职责：

```text
architecture.md - 说明整体架构、Agent 链路、Redis Stream、知识库和威胁情报
demo_guide.md - 说明样例日志演示和 WAF 日志演示流程
api_reference.md - 记录 /api/health、/api/analyze、/api/alerts/recent
troubleshooting.md - 记录 Docker、Redis、WAF、CORS、中文显示、前端无告警等问题
```

### 新增演示辅助脚本

已新增：

```text
scripts/clear_redis.py
scripts/demo_runbook.md
```

`clear_redis.py` 作用：

```text
清理 security:events
清理 security:alerts
清理 security:deadletter
清理 memory:ip:* 历史行为 Key
```

验证结果：

```text
python scripts\clear_redis.py
deleted redis keys: 18
```

### 新增测试用例

已新增：

```text
tests/test_modsecurity_parser.py
tests/test_log_parser_agent.py
tests/test_rag_agent.py
tests/test_orchestrator_context.py
tests/test_api_alerts.py
```

覆盖范围：

```text
ModSecurityParser 简化日志解析
LogParserAgent SQLi / Scanner 优先级与误报修复
RAGAgent 本地知识库检索
SecurityAnalysisOrchestrator 的 RAG + 情报 + Memory + 综合研判
GET /api/alerts/recent API 响应结构
```

### 依赖更新

已修改：

```text
backend/requirements.txt
```

新增：

```text
pytest>=8.0.0
```

### 验证结果

已执行编译检查：

```text
D:\anaconda\envs\machinelearning\python.exe -m compileall backend\app scripts tests
```

结果：

```text
编译通过
```

已执行测试：

```text
D:\anaconda\envs\machinelearning\python.exe -m pytest
```

结果：

```text
7 passed, 1 warning
```

说明：

```text
warning 来自 FastAPI / Starlette TestClient 的上游弃用提示，不影响当前功能。
```

已执行前端构建：

```text
cd frontend
npm run build
```

结果：

```text
TypeScript 检查通过
Vite build 通过
```

### Demo 链路验证

已执行：

```text
docker compose up -d redis
python scripts\clear_redis.py
python scripts\publish_sample_logs.py
cd backend
python -m app.services.event_consumer
```

验证结果：

```text
security:events = 4
processed events: 4
security:alerts = 4
```

抽查最新告警结果：

```text
attack_type = XSS
risk_level = high
evidence 包含 RAG 知识库引用
evidence 包含威胁情报
evidence 包含历史行为
evidence 包含综合研判
report_markdown 包含 RAG 知识库增强、威胁情报、历史行为、综合研判
```

### 当前阶段结论

第八阶段 MVP 已完成。

项目已从：

```text
功能可运行的安全分析 Demo
```

整理为：

```text
具备 README、架构文档、演示文档、API 文档、排障文档、测试用例和前端控制台的完整 SecRAG Agent 项目
```

## 第九阶段：真实 RAG 检索链路升级

### 阶段目标

本阶段目标是把原有偏轻量的本地知识库检索升级为更可解释的 RAG 检索链路。

重点从：

```text
简单本地知识匹配
```

升级为：

```text
结构化知识文档
KnowledgeChunk 切分
Query Rewrite
BM25 检索
HybridRetriever 混合检索框架
向量检索接口预留
引用来源、命中分数、命中原因输出
```

### 新增与调整的 RAG 数据结构

已修改：

```text
backend/app/rag/schemas.py
```

新增或增强：

```text
KnowledgeDocument
KnowledgeChunk
RetrievalResult
KnowledgeReference
RAGAnalysisResult
```

能力说明：

```text
KnowledgeDocument 表示原始安全知识文档
KnowledgeChunk 表示可检索知识片段
RetrievalResult 表示底层检索结果，包含 score、retrieval_type、reason
KnowledgeReference 表示进入告警报告的知识库引用
RAGAnalysisResult 增加 citations 字段，用于保留引用信息
```

### 知识库加载器增强

已修改：

```text
backend/app/rag/knowledge_loader.py
```

新增能力：

```text
load_documents() 加载结构化知识文档
load_chunks() 生成带 chunk_id、doc_id、category、tags 的知识片段
根据文件名推断 attack / mitre / waf_rule / cve / remediation 分类
根据内容自动提取 sqli、xss、path_traversal、command_injection、scanner、MITRE、WAF rule 等标签
```

### 新增 BM25 检索器

已新增：

```text
backend/app/rag/bm25_retriever.py
```

能力说明：

```text
实现轻量 BM25 排序
支持 top_k 检索
支持基础 metadata filters
输出 RetrievalResult
保留命中 terms 作为 reason
```

### 新增 HybridRetriever

已新增：

```text
backend/app/rag/hybrid_retriever.py
backend/app/rag/vector_retriever.py
```

能力说明：

```text
HybridRetriever 统一融合 BM25 和向量检索结果
当前 BM25 可用
VectorRetriever 作为后续 BGE + Milvus 的替换接口
最终输出 retrieval_type=hybrid 的检索结果
```

### Query Rewrite 增强

已修改：

```text
backend/app/rag/query_rewriter.py
```

新增能力：

```text
融合 attack_type
融合 URL path / query
融合 user_agent
融合 waf_rule_id 和 waf_message
融合 ParsedSecurityEvent 的 attack_features 和 evidence
根据 OWASP CRS 规则前缀扩展检索词
对查询词去重
```

示例：

```text
942100 -> OWASP CRS 942、sql injection、database security、parameterized query
941100 -> OWASP CRS 941、xss、cross site scripting、output encoding
930120 -> OWASP CRS 930、path traversal、directory traversal、arbitrary file read
```

### RAGAgent 改造

已修改：

```text
backend/app/rag/rag_agent.py
```

改造后的分析链路：

```text
ParsedSecurityEvent
  -> SecurityQueryRewriter
  -> HybridRetriever
  -> RetrievalResult
  -> KnowledgeReference
  -> RAGAnalysisResult
  -> SecurityAlert evidence / recommendations / report_markdown
```

告警增强内容：

```text
evidence 增加 RAG 引用
RAG 引用包含 source、score、reason
report_markdown 增加改写查询
report_markdown 增加知识库引用
report_markdown 增加知识库补充建议
```

### 新增测试

已新增：

```text
tests/test_knowledge_loader.py
tests/test_query_rewriter.py
tests/test_retrievers.py
```

已增强：

```text
tests/test_rag_agent.py
```

测试覆盖：

```text
KnowledgeLoader 可以加载结构化文档和 chunk
chunk 包含 chunk_id、doc_id、category、tags
BM25Retriever 可以命中 SQL Injection 知识
HybridRetriever 可以返回 source、score、reason
QueryRewriter 可以扩展 attack_type、rule_id、MITRE、sqlmap 等检索上下文
RAGAgent 输出 citations、score、reason 和 recommended_actions
```

### 文档更新

已修改：

```text
README.md
docs/architecture.md
```

更新内容：

```text
README 增加第九阶段完成状态
README 将 RAG 描述从轻量检索升级为 Query Rewrite + BM25 + HybridRetriever
architecture.md 增加 RAG 内部链路说明
architecture.md 说明 VectorRetriever 作为 BGE + Milvus 的扩展接口
```

### 验证结果

已执行编译检查：

```text
D:\anaconda\envs\machinelearning\python.exe -m compileall backend\app tests
```

结果：

```text
编译通过
```

已执行测试：

```text
D:\anaconda\envs\machinelearning\python.exe -m pytest
```

结果：

```text
11 passed, 1 warning
```

说明：

```text
warning 来自 FastAPI / Starlette TestClient 的上游弃用提示，不影响当前功能。
```

### 当前阶段结论

第九阶段已完成真实 RAG 检索链路升级。

项目的 RAG 能力已从：

```text
简单本地 Markdown 知识匹配
```

升级为：

```text
具备结构化文档、Query Rewrite、BM25 召回、HybridRetriever 框架、引用来源、命中分数和命中原因的可解释 RAG 检索链路
```

## 第十阶段：成本可控的安全分析流水线与可解释评分

### 阶段目标

本阶段目标是从产品落地角度优化 SecRAG Agent，不继续堆叠多 Agent 概念，而是让系统具备更适合 SOC 场景的成本控制和可解释能力。

核心思路：

```text
规则优先
RAG 按需
情报按需
Memory 按需
评分可解释
分析耗时可观测
```

### 新增 analysis 模块

已新增：

```text
backend/app/analysis/__init__.py
backend/app/analysis/schemas.py
backend/app/analysis/policy.py
backend/app/analysis/score_breakdown.py
backend/app/analysis/timer.py
```

模块职责：

```text
schemas.py - 定义 AnalysisMode、RiskScoreItem、RiskScoreBreakdown、AnalysisMetadata
policy.py - 根据基础风险选择 fast / enriched / deep 分析模式
score_breakdown.py - 生成和追加评分拆解
timer.py - 记录单次分析耗时
```

### 分析模式设计

已实现三种分析模式：

```text
fast
enriched
deep
```

模式策略：

```text
fast - 低风险事件，仅执行日志解析和基础决策，跳过 RAG、ThreatIntel、Memory
enriched - 中风险事件，执行 RAG 和 Memory，跳过 ThreatIntel
deep - 高危事件，执行 RAG、ThreatIntel、Memory 全量增强
```

### SecurityAlert 扩展

已修改：

```text
backend/app/models/alert.py
```

新增字段：

```text
analysis_mode
score_breakdown
analysis_metadata
```

字段含义：

```text
analysis_mode - 当前告警采用 fast / enriched / deep 哪种分析模式
score_breakdown - 风险评分拆解，说明每个分数来源
analysis_metadata - 启用模块、跳过模块、分析耗时、RAG/情报/Memory 是否使用
```

### DecisionAgent 改造

已修改：

```text
backend/app/agents/decision_agent.py
```

新增能力：

```text
生成基础 RiskScoreBreakdown
将攻击类型基础分、WAF 状态、WAF 规则命中、扫描器特征拆成独立评分项
每个评分项包含 name、score、reason、source
```

示例评分项：

```text
Attack type baseline +82
WAF blocked request +4
WAF rule hit +6
Automated scanner feature +8
```

### Orchestrator 改造

已修改：

```text
backend/app/agents/orchestrator.py
```

改造后的链路：

```text
SecurityEvent
  -> LogParserAgent
  -> DecisionAgent
  -> AnalysisPolicy
  -> fast / enriched / deep
  -> 按模式执行 RAG / ThreatIntel / Memory
  -> 写入 AnalysisMetadata
  -> 写入 RiskScoreBreakdown
  -> 生成最终 SecurityAlert
```

实现效果：

```text
低风险请求不会触发不必要的 RAG 和情报查询
中风险请求会补充 RAG 知识和历史行为
高危请求会触发完整增强
最终报告会追加“分析模式与评分解释”
```

### 前端展示增强

已修改：

```text
frontend/src/types/alert.ts
frontend/src/components/AlertDetail.tsx
frontend/src/styles/index.css
```

新增展示内容：

```text
Mode
Latency
Analysis Enabled Modules
Analysis Skipped Modules
Score Breakdown
```

前端详情页现在可以直接展示：

```text
本次告警走 fast / enriched / deep 哪条路径
启用了哪些增强模块
跳过了哪些高成本模块
每个风险分数来自哪里
单次分析耗时多少
```

### 新增测试

已新增：

```text
tests/test_analysis_policy.py
tests/test_orchestrator_analysis_mode.py
```

测试覆盖：

```text
AnalysisPolicy 可以选择 fast / enriched / deep
fast 模式跳过 RAG、ThreatIntel、Memory
enriched 模式执行 RAG 和 Memory，跳过 ThreatIntel
deep 模式执行 RAG、ThreatIntel、Memory
SecurityAlert 返回 analysis_mode、score_breakdown、analysis_metadata
report_markdown 包含分析模式与评分解释
```

### 验证结果

已执行后端编译检查：

```text
D:\anaconda\envs\machinelearning\python.exe -m compileall backend\app tests
```

结果：

```text
编译通过
```

已执行后端测试：

```text
D:\anaconda\envs\machinelearning\python.exe -m pytest
```

结果：

```text
16 passed, 1 warning
```

已执行前端构建：

```text
cd frontend
npm run build
```

结果：

```text
TypeScript 检查通过
Vite build 通过
```

说明：

```text
warning 来自 FastAPI / Starlette TestClient 的上游弃用提示，不影响当前功能。
```

### 当前阶段结论

第十阶段已完成。

项目已从：

```text
所有可疑事件都走同一条增强链路
```

升级为：

```text
根据风险等级动态选择 fast / enriched / deep 的成本可控安全分析流水线，
并在告警和前端中输出评分拆解、启用模块、跳过模块和分析耗时。
```

## 第十一阶段：企业上下文 RAG 与 AutoTriage 自动分流

### 阶段目标

本阶段目标是让 SecRAG Agent 不只根据攻击类型和风险分数分析告警，还能结合企业内部上下文进行自动分流。

核心问题：

```text
这个接口属于哪个业务？
负责人是谁？
目标资产是否高危？
源 IP 是否内部扫描器？
当前 WAF 规则是 block 还是 detect？
是否需要人工复核？
```

### 新增企业上下文数据

已新增：

```text
backend/app/data/context/asset_inventory.md
backend/app/data/context/service_owners.md
backend/app/data/context/scanner_whitelist.md
backend/app/data/context/waf_policy.md
backend/app/data/context/incident_playbook.md
backend/app/data/context/change_calendar.md
```

覆盖内容：

```text
资产清单
业务负责人
内部扫描器白名单
WAF 策略
应急预案
变更窗口
```

### 新增 Context 模块

已新增：

```text
backend/app/context/__init__.py
backend/app/context/schemas.py
backend/app/context/context_loader.py
backend/app/context/context_retriever.py
backend/app/context/context_agent.py
```

能力说明：

```text
ContextLoader 加载企业上下文 Markdown
ContextRetriever 根据 source_ip、target、attack_type、waf_rule_id 检索上下文
ContextAgent 提取 business_owner、asset_name、asset_criticality、is_internal_scanner、waf_action、change_window
```

### 新增 AutoTriage 模块

已新增：

```text
backend/app/triage/__init__.py
backend/app/triage/schemas.py
backend/app/triage/auto_triage_policy.py
```

支持状态：

```text
auto_triaged
needs_review
investigating
resolved
false_positive
```

支持自动化决策：

```text
observe_only
auto_close
notify_owner
block_ip_recommended
human_review_required
```

### SecurityAlert 扩展

已修改：

```text
backend/app/models/alert.py
```

新增字段：

```text
status
automation_decision
triage_reason
requires_human_review
business_owner
asset_name
asset_criticality
context_references
analyst_note
handled_by
handled_at
```

### Orchestrator 接入

已修改：

```text
backend/app/agents/orchestrator.py
```

当前链路：

```text
SecurityEvent
  -> LogParserAgent
  -> DecisionAgent
  -> AnalysisPolicy
  -> RAG / ThreatIntel / Memory
  -> ContextAgent
  -> AutoTriagePolicy
  -> SecurityAlert
```

实现效果：

```text
ContextRAG 和 AutoTriage 作为轻量模块参与所有告警分流
内部扫描器低风险告警可以 auto_close
critical 资产高风险告警会进入 needs_review
告警报告包含企业上下文和 AutoTriage 结论
```

### 前端展示增强

已修改：

```text
frontend/src/types/alert.ts
frontend/src/components/AlertDetail.tsx
frontend/src/styles/index.css
```

新增展示：

```text
Status
Automation Decision
Requires Human Review
Business Owner
Asset Name
Asset Criticality
Triage Reason
Context References
```

### 新增测试

已新增：

```text
tests/test_context_triage.py
```

已调整：

```text
tests/test_orchestrator_analysis_mode.py
```

测试覆盖：

```text
ContextAgent 可以识别 /login 的资产、负责人、资产等级和 WAF 策略
ContextAgent 可以识别内部扫描器
AutoTriagePolicy 可以让 critical 资产进入人工复核
AutoTriagePolicy 可以自动关闭低风险内部扫描器告警
Orchestrator 元数据包含 ContextRAG 和 AutoTriage
```

### 验证结果

已执行后端编译检查：

```text
D:\anaconda\envs\machinelearning\python.exe -m compileall backend\app tests
```

结果：

```text
编译通过
```

已执行后端测试：

```text
D:\anaconda\envs\machinelearning\python.exe -m pytest
```

结果：

```text
20 passed, 1 warning
```

已执行前端构建：

```text
cd frontend
npm run build
```

结果：

```text
TypeScript 检查通过
Vite build 通过
```

说明：

```text
warning 来自 FastAPI / Starlette TestClient 的上游弃用提示，不影响当前功能。
```

### 当前阶段结论

第十一阶段已完成。

项目已从：

```text
只基于安全知识和风险分数生成告警
```

升级为：

```text
结合企业资产、负责人、内部扫描器白名单、WAF 策略和预案上下文，对告警进行自动分流，并判断是否需要人工复核。
```
